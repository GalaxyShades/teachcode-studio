# Isolated local demo only; shared deployment uses .env.production.example.
DATABASE_MODE=sqlite
SQLITE_DATABASE_PATH=./data/teachcode-content-studio.local.db
APP_URL=http://localhost:3000
ALLOWED_CORS_ORIGINS=http://localhost:3000
